{% include 'header.twig' %}
<h2>{{ translations.alliance }}</h2>
<form method="post">
    <fieldset>
        <legend>{{ translations.main }}</legend>
        <div class="split2">
            <label>{{ translations.name }}<input type="text" name="name"/></label>
            <label title="ts8.x1.europe.travian.com">{{ translations.world }}*<input type="text" name="world"/></label>
        </div>
        <input type="submit"/>
    </fieldset>
</form>
{% include 'footer.twig' %}
